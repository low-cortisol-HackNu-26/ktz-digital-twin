FROM python:3.12-slim

WORKDIR /app

COPY dispatcher/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY dispatcher/app app

ENV PYTHONPATH=/app

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8002"]
