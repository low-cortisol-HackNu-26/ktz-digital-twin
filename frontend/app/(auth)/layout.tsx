// Auth route group layout.
//
// Client component — checks cardAuth.isAuthenticated() on mount.
// If token is valid → redirect to /dashboard (user already logged in).
// Renders full-screen dark background for card tap prompt.
// Shows KTZ logo and "Digital Twin" tagline centered above the tap UI.
// Mark with 'use client'.
