// Per-locomotive dashboard page — same layout as /dashboard/page.tsx
// but locomotiveId is taken from params.locomotiveId (dynamic route).
//
// Passes locomotiveId to all panels and to the WS hook.
// Useful for direct links / bookmarking a specific locomotive.
